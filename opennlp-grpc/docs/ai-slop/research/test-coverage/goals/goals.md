# Goals: test coverage

Effort: S under a day, M a few days, L a week or more.

## P1

- [x] (S) Fix ordinal sentiment labels in `visualization-data.ts:162-174`; derive
      polarity by rank, show the label, drop "cosine" from the sentiment lane
      (`document-heatmap-view.ts:104,128`); red test first with `1_star`..`5_stars`.
- [x] (S) Scale the `index-documents` deadline by input size
      (`GrpcSearchRpc.java:107`), same function as `GrpcAnalysisRpc`.
- [x] (S) Vitest: every FE enum literal and every `api.ts` key exists in the
      descriptor set `opennlp-grpc-v1.protobin`.
- [x] (M) Request fixtures generated by the real builders
      (`buildAnalysisRequest`, `createSearchRequest`, `createCompoundSearchRequest`,
      the `IndexDocumentsRequest` assembly) under `test/fixtures/requests/`;
      `FrontEndRequestFixtureTest` in the gateway parses each with the strict
      parser and runs the static validators (`DynamicSearchIndexRegistry.validateEmbedding`).
- [x] (M) Playwright smoke inside `docker/test-image.sh` with a novel-sized
      payload; invoke the script from CI or a release checklist.
- [ ] (M) Pin the sentiment label set of the catalog model at the integration
      layer; export the set from the FE and assert it in a vitest.

## P2

- [ ] (M) Size ladder test for `scaledDeadlineNanos` and the gateway routes that use it.
- [x] (M) e2e specs for Models & data and Lifecycle (both have none); Trainer got one too.
- [ ] (S) Gateway test for `/api/v1/model-bundles`; FE tests for the NDJSON stream readers.
- [ ] (L) Integration tests past `search-indexes` and `search`: analyze, index,
      persist, seal, alias, collection, catalog install.
- [ ] (S) Replace the echo-mock in `test/api.test.ts` with assertions against the fixtures above.

## P3

- [ ] (L) Generate TypeScript request types from the descriptor set.
- [ ] (M) A seam in `main.ts` so tab wiring can be unit tested.
- [ ] (S) Make `-Dopennlp.forkCount` real for this subtree or remove it from
      `opennlp-grpc-webapp/README.md:17`.
- [ ] (S) A native-image smoke test (boot, health, one analyze) in `docker/native`.
